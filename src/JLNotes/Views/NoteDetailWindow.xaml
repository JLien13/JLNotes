<Window x:Class="JLNotes.Views.NoteDetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Note Detail"
        Width="400" Height="500"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize">

    <Border Background="{DynamicResource BgPrimaryBrush}"
            BorderBrush="{DynamicResource BorderSubtleBrush}"
            BorderThickness="1" CornerRadius="8" Padding="16"
            MouseLeftButtonDown="Border_MouseLeftButtonDown">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Title -->
            <TextBox Grid.Row="0" Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}"
                     Background="{DynamicResource BgSecondaryBrush}"
                     Foreground="{DynamicResource TextPrimaryBrush}"
                     BorderBrush="{DynamicResource BorderSubtleBrush}"
                     FontFamily="Segoe UI" FontSize="15" FontWeight="SemiBold"
                     Padding="8,6" Margin="0,0,0,8" />

            <!-- Project + Priority row -->
            <Grid Grid.Row="1" Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Project" Style="{StaticResource SecondaryText}" Margin="0,0,0,2" />
                    <TextBox Text="{Binding Project, UpdateSourceTrigger=PropertyChanged}"
                             Background="{DynamicResource BgSecondaryBrush}"
                             Foreground="{DynamicResource TextPrimaryBrush}"
                             BorderBrush="{DynamicResource BorderSubtleBrush}"
                             Padding="6,4" />
                </StackPanel>
                <StackPanel Grid.Column="1" Margin="8,0,0,0">
                    <TextBlock Text="Priority" Style="{StaticResource SecondaryText}" Margin="0,0,0,2" />
                    <ComboBox SelectedItem="{Binding PriorityText}"
                              Style="{StaticResource DarkComboBox}">
                        <sys:String xmlns:sys="clr-namespace:System;assembly=System.Runtime">High</sys:String>
                        <sys:String xmlns:sys="clr-namespace:System;assembly=System.Runtime">Medium</sys:String>
                        <sys:String xmlns:sys="clr-namespace:System;assembly=System.Runtime">Low</sys:String>
                    </ComboBox>
                </StackPanel>
            </Grid>

            <!-- Branch -->
            <StackPanel Grid.Row="2" Margin="0,0,0,8">
                <TextBlock Text="Branch" Style="{StaticResource SecondaryText}" Margin="0,0,0,2" />
                <TextBox Text="{Binding Branch, UpdateSourceTrigger=PropertyChanged}"
                         Background="{DynamicResource BgSecondaryBrush}"
                         Foreground="{DynamicResource TextPrimaryBrush}"
                         BorderBrush="{DynamicResource BorderSubtleBrush}"
                         Padding="6,4" />
            </StackPanel>

            <!-- Repo -->
            <StackPanel Grid.Row="3" Margin="0,0,0,8">
                <TextBlock Text="Repo" Style="{StaticResource SecondaryText}" Margin="0,0,0,2" />
                <TextBox Text="{Binding Repo, UpdateSourceTrigger=PropertyChanged}"
                         Background="{DynamicResource BgSecondaryBrush}"
                         Foreground="{DynamicResource TextPrimaryBrush}"
                         BorderBrush="{DynamicResource BorderSubtleBrush}"
                         Padding="6,4" />
            </StackPanel>

            <!-- Body -->
            <TextBox Grid.Row="4" Text="{Binding Body, UpdateSourceTrigger=PropertyChanged}"
                     Background="{DynamicResource BgSecondaryBrush}"
                     Foreground="{DynamicResource TextPrimaryBrush}"
                     BorderBrush="{DynamicResource BorderSubtleBrush}"
                     FontFamily="Segoe UI" FontSize="12"
                     AcceptsReturn="True" TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     Padding="8,6" />

            <!-- Buttons -->
            <StackPanel Grid.Row="5" Orientation="Horizontal"
                        HorizontalAlignment="Right" Margin="0,12,0,0">
                <Button Content="Cancel" Style="{StaticResource SubtleButton}"
                        Command="{Binding CancelCommand}" Margin="0,0,8,0" />
                <Button Content="Save" Style="{StaticResource SubtleButton}"
                        Command="{Binding SaveCommand}"
                        Foreground="{DynamicResource AccentBlueBrush}" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
