# Tradeshow Leads — Spencer Lien, 05/07/2026

No `corvascular.trade_show` rows in the last 180 days, so no first-touch list
this run.

If a recent show happened and the leads are sitting in spreadsheets / email
folders rather than the CRM, push them into `corvascular.trade_show` (or have
the booth team capture into the admin `/admin/customer-data/tradeshow` page)
so they show up here next run.

For the next iteration: also pull `quote_requests.source` = trade-show-tagged
rows and any `organizations.source` containing "tradeshow" or "show" — those
are the same lead population in a different table.
